# AGENTS.md

このリポジトリは GPT-2 124M の推論を Numo::NArray / Cumo だけで実装するプロジェクト。進め方は docs/plans/PLAN-gpt2.md に従う。**前の段階の受け入れテストが通るまで次の段階のコードを書かないこと。**

## バックエンドの切り替え

既定は Numo (CPU)、`GPU=1` で Cumo。コードは `XM` 定数経由で書き、`Numo::` / `Cumo::` を直接書かない。**フォワードパスのテンソルは `XF` 定数で作る** (`XM::SFloat` を直接書かない)。

```ruby
if ENV['GPU'].to_s =~ /\A(1|on|true)\z/i
  require 'cumo/narray'
  XM = Cumo
else
  require 'numo/narray'
  XM = Numo
end
```

- 実装の dtype は `XF` 定数で、既定は `XM::SFloat` (fp32)。受け入れテストがトークン列の完全一致なので、**既定をこれ以外にしない**。DFloat は使わない (コンシューマ GPU の fp64 は 1/64 レートで、比較の意味がなくなる)。**分岐する dtype は版によって変わる。** 当初ここには「fp16 は 3 トークン目に分岐する (融合カーネルが入る前は 2 トークン目)」と書いていたが、**2026-09-20 に測り直したら fp16 は GPT-2 の 256 トークンすべてで fp32 と一致し、代わりに bf16 が 3 個目で分岐した** (717 対 471)。**「fp16 だから壊れる」ではなく「そのときの版で確かめる」もの** で、既定を動かさない理由は **確かめずに済ませられないこと** のほうにある。
- **`Cumo::HFloat` (fp16) は計測対象に含める。** 版が上がるたびに fp32 と並べて測り、結果は docs/results/ の実装ごとのファイル (GPT-2 124M なら docs/results/gpt2-124m.md) の「fp16 で測るとどうなるか」に置く。CuPy と PyTorch も同じ節で fp16 を測ってある。測るときに必ず踏むものが 2 つある。(a) layernorm で `(x - mean)` の二乗を fp16 の配列として作ると Inf になる (GPT-2 の外れ値で fp16 上限の 142 倍に達する)。`var` は fp32 で畳むのでこれを避けられるが、**上限を免れるのではなく収まっているだけ** で余裕は 5 倍しかない。(b) トークン id は 2048 を超えると fp16 で値が飛ぶので、one-hot は `SFloat` で作ってから cast する。
- **fp16 に切り替えるのは `DTYPE=fp16` で、モンキーパッチは要らない** (`GPU=1 DTYPE=fp16 ruby script/gpt2_generate.rb`)。Numo に半精度は無いので `GPU=1` と併用する。**`DTYPE=bf16` で `Cumo::BFloat` も選べる。** **上の (a) と (b) は `Ops` が `XF` で分岐して済ませる。** 因果マスクの `-1.0e9` は fp16 で `-Infinity` になるので `-1.0e4` に、`exp` の clip は 88.0 では Inf になるので 11.0 になる (実測で `exp(11.0)` が 59872、`exp(11.2)` が Inf)。**重みは fp32 のまま読み**、`Ops.contiguous` の `new + store` が載せ替えを兼ねる。(a) のフォールバックは融合カーネルが無い版では Inf になるので、`layernorm` と `rmsnorm` は黙って答えずに例外にしてある。**Python の 2 実装も `DTYPE=fp16` を受ける** (`GPU=1 DTYPE=fp16 python/.venv/bin/python python/bench_gpt2.py --impl torch`)。numpy にも cupy にも bfloat16 は無いので、**3 実装を並べて測れるのは fp16 まで** である。
- **bf16 は fp16 と壊れ方が違うので、fp16 の分岐を流用しないこと。** 指数部が fp32 と同じなので上の (a) も因果マスクの `-1.0e4` も `exp` の clip も要らない (`-1.0e9` は -998244352.0 のまま、`exp(88.0)` は 1.65e38)。**代わりに (b) が重い。** 仮数部が 8 ビットしかなく、**トークン id は 256 で飛ぶ** (50256 が 50176 になる)。fp16 の 2048 より 8 倍早い。`Ops` の分岐は `fp16?` (マスクと clip) と `reduced_precision?` (one-hot の cast) で分けてあり、**後者は 16 ビットの両方に掛かる**。
- CI や開発マシンに GPU が無くても、Numo で全テストが通る状態を常に保つ。

## Numo と Cumo の差異 (このプロジェクトで実際に踏むもの)

### スカラー: Cumo の縮約は 0 次元 NArray を返す

`sum` / `max` / `min` などは Numo では Ruby の Numeric を、Cumo では 0 次元 NArray を返す。読み戻しは `NArrayLLM.scalar` (`lib/narray_llm/backend.rb`) に 1 箇所だけ置いてある。

**ただしループの中では読み戻さない。** 0 次元 NArray はそのまま配列との四則演算に使える。読み戻し (extract) はキューが空になるまで待つため、毎反復呼ぶとパイプラインが完全に断ち切られる。実測では、読み戻し自体は 0.2 µs と無料だが、それが誘発する待ちで反復あたり数十〜数百 µs 損する。読み戻しは「表示するとき」「収束判定するとき」だけにし、判定は間引く。

### min_index / max_index は平坦化した通し番号を返す

numpy の argmin/argmax と違い、`min_index(axis: 1)` は多次元配列を平坦化した通し番号を返す。行ごとのラベルにするにはオフセットを引く:

```ruby
idx = d.min_index(axis: 1)          # [N] だが値は 0...(N*K) の通し番号
labels = idx - idx.class.new(N).seq * K
```

### インデックス gather は、NArray を添字にしたときだけ同期する

NArray をインデックスに使うと、Cumo はインデックス配列をブロッキング cudaMemcpy でホストに読み戻して範囲チェックする (`ext/cumo/narray/index.c`)。キュー全体の完了を待つため、ループ内で使うと極端に遅くなる。**`CUMO_SHOW_WARNING=ON` で `cumo_na_parse_narray_index` の警告が出る。** 当初ここには「警告が出ない」と書いていたが、**いまの cumo は出す** (index.c:266)。

- **Ruby の Array を添字にすれば同期しない** (`a[ruby_array, true]`)。読み戻すデバイスデータが無いため。Integer 1 個 (`a[i, true]`) も同じ。
- **トークン埋め込みはこの gather で書く。** 当初ここには「gather ではなく one-hot 行列 x wte の GEMM で書く」と書いていたが、**それは NArray 添字しか見ていない判断だった**。`ids` は Ruby の Array のまま届くので gather が使え、キャッシュ無しで 1.11〜1.31 倍速い (docs/results/)。
- **添字をデバイス上に置いたまま引きたいときは、この同期を踏む。** ONNX の Gather や MoE のエキスパート選択がそれで、そちらは別の問題として残っている。

### Bit 配列・マスク代入・fancy index を避ける

比較演算 (`a > b`) は Bit を返すが、Bit 経路は Cumo で非互換や性能問題を踏みやすい。0/1 のフラグが欲しいときは算術で作る:

```ruby
escaped = ((x - threshold).clip(0.0, 1.0)).ceil   # x > threshold で 1.0
mask    = 1.0 - escaped                            # 論理否定
combined = a * mask + b * (1.0 - mask)             # マスク合成
```

因果マスクも同様に算術で作ってある (`Ops.causal_mask`)。**埋める値は `-1.0e9` 直書きではなく `Ops::MASK_VALUE`** で、fp16 では `-1.0e4` に切り替わる (上記)。

### 非連続ビューと dot

ヘッド分割の列スライス (`q[true, h*hs...(h+1)*hs]`) は非連続ビュー。`dot` に渡す前に `Ops.contiguous` で連続化する。**中身は `zeros` ではなく `new + store`** で、`store` が全要素を上書きするので `zeros` の fill カーネルは無駄になる (作法 19 に、これが効く条件と効かない条件がある)。**第 2 引数に移し先の dtype を取れる** ので、fp32 で回したい前段 (Whisper のメル) はそこで指定する。

非連続ビューの store/copy/要素演算は cumo #245/#246/#248/#251/#252 で大幅に改善されたが、依存する cumo のバージョンによっては行ごとカーネル起動に落ちて 100 倍以上遅くなる。性能異常を見たらまず cumo のバージョンを確認する。

**ただし `dot` 以外では、連続化が要らないことのほうが多い。**`[100000]` の縮約とソートを 20 回ずつ nsys で数えた (2026-09-20)。

| 受け手 | `sum` 20 回 | `sort` 20 回 |
|---|---|---|
| 連続 | 45 本 | 165 本 |
| ストライドだけのビュー (`a[1..-1]`) | **45 本** | **165 本** |
| `reverse` | **45 本** | **165 本** |
| **添字配列のビュー (`a[idx]`)** | **85 本** | — |

- **ストライドだけのビューは、縮約も要素演算もそのまま受ける。**`Ops.contiguous` を挟むと **払わなくてよい 1 本を払う**
- **CUB を通る経路 (`sort` / `sort_index` / `median`) は、受け手が連続でも非連続でも同じ本数である。**`sort` は入力を壊すので **必ず 1 本コピーを出す** (`cumo_na_copy`)。**外から連続化しても減らない**
- **添字配列を持つビューだけは別で、縮約 1 回につき 2 本多い** (85 対 45)。`reverse` や `a[1..-1]` と同じ扱いにしないこと

**`dot` に渡すときも、連続化するかどうかは形で決まる。** 転置ビューは F 連続なので、cumo の `make_gemm_layout` が **コピーせず `CUBLAS_OP_T` に渡す**。**そのほうが速いとは限らない。** 別セッションが 2 次元 (`batch_count=1`) で 8 形を測った (同一プロセスで交互、クロック固定、9 ラウンドの中央値、2 回再現)。

| 形 (M, K, N) | `OP_T` / (コピー + `OP_N`) | |
|---|---|---|
| 1, 64, 1500 (Whisper decoder) | **0.65** | **`OP_T` が 2.0 倍得** |
| 512, 64, 512 (Switch) | 1.07 | `OP_T` が得 |
| 1500, 64, 1500 (Whisper encoder) | 1.19 | コピーが 11% 得 |
| 256, 3072, 768 (GPT-2 backward) | 1.43 | コピーが 21% 得 |
| 4096, 64, 4096 | 1.30 | コピーが 22% 得 |

**コピーの費用は `N*K`、`OP_T` の罰は `M*N*K` に比例するので、M で決まる。** M が小さいほど `OP_T` が得で、**M=1 では無条件に連続化すると 2 倍損する**。**ヘッド分割の列スライスはストライドだけのビューで F 連続ではない** ので、そちらは従来どおり連続化する (cumo は C 連続でも F 連続でもない operand を `dup` する)。

**この表は 2 次元のものなので、3 次元のバッチ経路 (`is_batched_f_contiguous` の枝) に当てないこと。** そちらは別に測ってあり、`Ops.attention` の形 (M=T=64, K=64) で **カーネルは 2 本から 1 本に減るのに wall は動かなかった** (docs/measurement-cases.md 11)。

### gemm の答えは inplace のフラグを持って返る

`Ops.linear_add` は Cumo では `x.gemm(weight_t, c.inplace, beta: 1)` に落ちる (Numo には `gemm` が無いので `dot` + 加算になる)。**返ってくる配列は C とは別オブジェクトなのに、`inplace?` が真で、C とバッファを共有している。**

```ruby
r = a.gemm(b, c.inplace, beta: 1)
r.inplace?      # => true
t = r * s       # r を書き換えて、t は r と同一オブジェクトになる
```

**フラグは消費されずに下流へ流れる。**`(x * scale) + shift` のような続きを書くと、**そのたびに同じバッファが書き換わる**。答えを 1 度しか読まないなら害は出ないが、**2 度読む側にとっては罠である。** 2026-09-21 に踏んだ。ResNet の中間活性を参照と比べたら、**比較の式 `(y - 参照)` が `y` を書き換えて**、その `y` を次の段に渡していた。**モデルは正しく、壊していたのは検査のほうだった。**

**外に出す前に `view` を通すとフラグが落ちる。** バッファは共有したままなので **カーネルを払わない** (`out.view.reshape!(...)`)。`dup` はコピーになる。**gemm の答えを呼び出し元に返す関数を書くなら、`view` を通すこと。**

### ビューにスカラーを書くなら `fill` を使う

**cumo 0.10.0 では `a[...] = 0.0` と `a.store(0.0)` が 1 回で 2 本になる。** スカラーをいったん 0 次元配列にしてから store するので、**カーネル 1 本とプールからの確保 1 回が余分に掛かる。**`a[...].fill(0.0)` は値をカーネル引数で渡すので 1 本で済む。**cumo #532 (master `2a14408e`、未リリース) で直り、スカラー代入も 1 本になる** (別セッションの報告。こちらでは確かめていない)。**0.10.0 を使う間は `fill` のままにしておくこと。** 直った版でも `fill` は損をしない。2026-09-23 に ResNet のパディングで踏み、1 パスが 384 本で済むところを 456 本払っていた。**ループの中で縁や一部を埋めるなら、ビューに `fill` を掛けること。**

### 乱数

Numo と Cumo は乱数生成器が別物で、同じ seed でも **系列は一致しない**。両バックエンドの結果を比較するテストで乱数入りの入力が要る場合は、決定的な式 (seq を使った算術) で作るか、片方で生成して to_binary / from_binary で渡す。「同一 seed だから一致するはず」を前提にしない。

### NaN / Inf

fp32 で `exp` に大きい値を渡すと Inf、Inf - Inf で NaN になり、clip は NaN を素通しする。`exp` の引数は事前に clip する (softmax は max を引く定番の安定化を必ず入れる)。

## 計測の作法 (このリポジトリのベンチ全部に適用)

**実例と、外した経緯は [docs/measurement-cases.md](docs/measurement-cases.md) にある。** 番号が対応しているので、則の理由が要るときはそちらを引く。

1. **best-of-N (N>=3) を必ず使う。** 単発測定は GPU クロックの立ち上がりや glibc の確保挙動で数倍ぶれる。
2. Cumo の時間計測は反復の区切りで `XM::CUDA::Runtime.cudaDeviceSynchronize` を呼ぶ。呼ばないとカーネル投入時間しか測れない。
3. ただし **区間ごとの同期は全体を遅くする**。内訳を取るモード (DETAIL=1) と総時間だけのモード (DETAIL=0) を分け、報告する総時間は必ず DETAIL=0 の値。
4. ウォームアップを入れる (初回は cuBLAS ハンドル生成・JIT・ページフォルトが乗る)。**何のためのウォームアップかで単位が変わる。** 確保や JIT を踏ませるのが目的なら **演算の回数** で数える。クロックや熱を定常にするのが目的なら **壁時計** で数える。**固定回数のウォームアップは、速いアームだけを系統的に冷やす** — 2.4 倍速いアームは同じ反復回数で 2.4 分の 1 しか温まらない (別セッションの観測。こちらでは追試していない)。
5. 比較表に「1 演算ごとに同期した値」を載せる場合は、その旨を明記する。パイプラインが効く実使用とは分岐点が変わる。
6. Numo 側の GEMM は素の `dot` だと BLAS を使わない。性能比較に使う場合は numo-linalg (または numo-linalg-alt) を入れ、表に BLAS の有無を明記する。
7. **プロセス間のばらつきは best-of-N では潰せない。潰せるのはプロセス内だけ。** **幅は比較のたびに測り直す。前回の幅を流用して「今回の差は幅の外だ」と言ってはいけない。** 幅そのものが測るたびに動き、条件ごとの大小の順位まで入れ替わる。言えるのは **1 回が約 2 秒の条件だけが 0.4〜1.7% に収まり、1 秒未満は 3.0〜11.2% ぶれる** まで。**短い条件で数 % の差を「改善」として書かないこと。** 条件をブロックで回すと熱ドリフトが条件と相関するので、**複数条件はインターリーブして測る**。**符号が数ラウンド揃っただけで結論にしないこと。差を主張するなら 10 ラウンド取り、ペア比の最小と最大が 1 をまたぐかどうかで見る。** **ただし「またぐ」は大きさが決まらないという判定で、向きが決まらないという判定ではない。** 幅が効果より大きければ範囲は必ずまたぐが、**向きはそれとは独立に決まりうる**。**符号検定を併せて出すこと** — 24 ラウンド中 22 本が片側なら両側 p は 3.6e-05 である。**帰無分布は対照ペアが与える** (同じビルドどうしが 15/24 なら、本命の 22/24 は偶然ではない)。**またぐからと黙るのではなく、確度を明記して渡すこと。** 2026-09-21 にこれで cumo の退行を 1 つ落としかけた (measurement-cases 7)。 **そして分布の形 (二峰かどうか) を読むのは、差を読むより多くの点が要る。** 隙間を見るなら **必ず幅で割る** — 絶対値の隙間は幅に比例するので、幅が 2 倍違う 2 条件をそのまま比べても何も言えない。**そして 10 点では足りない。** 単峰分布から 10 点取ったときの「最大隙間 / 幅」は中央値 30%、**90% 点が 44%** で、**ほぼ何でも「隙間がある」ように見える** (n=15 なら 90% 点は 35%)。**記録は並べ替える前の順序で残すこと。** 昇順で書いてしまうと、2 つの塊が「前半と後半」なのか「交互」なのかが **永久に失われる** — 段差で条件が変わったのか走行ごとに行き来しているのかを、後から誰も決められなくなる。
8. **nsys の差分法は、2 本とも「温まった長さ」で走らせる。** 1 トークンあたりの値は 2 つの生成長の差を取って割り出すが (`--length 72` と `--length 40` の差 ÷ 32)、**短いほうを冷えたまま走らせると時間が過小に出る**。**回数には影響しない** ので、回数だけが目的なら短くてよい。
9. **nsys の時間は行ごとに推定量を選ぶ。原則は「平均 × 本数」。** 例外が両方向にある。(a) モデル読み込み時のページフォルトを踏む行は **平均が壊れる** ので中央値基準にする (`Max / Med > 100` を目安に判定できる)。(b) 逆に **複数の形が混ざる行に中央値を使ってはいけない**。**そして時間は 1 本の profile では決まらない。回数は動かないが、時間は複数本取って外れを見分けること。** そして **その演算を消して wall が動くまで、行の時間を信じないこと。** nsys の duration は wall の仕事とは限らない。**この判定は差分法を使っていても要る。** 消えるのは「一度きりの費用」だけで、**長さに比例して増える費用は差を取っても残る**。**1 トークンあたりの費用が欲しいなら、推定量を選ぶより総時間の差分を取るほうが正しい。** 9(b) が平均を勧めるのは「中央値が 1 つの形の値でしかない」からで、差分法はその問題を持たない。**ただし差分法は分母が小さいと使えない** — 差が全体の 44% なら安定するが、11% では倍近く散り、変更が触らない行まで 5.5 倍に散る。**差が全体に対して小さいときは行ごとの時間を読んではいけない。** **そして 2 つの実装を nsys の時間で比べるなら、相手と比べる前に自分を 3 組測ること。** **1 組で出た比は、相手との差ではなく自分の幅かもしれない。幅より小さい差を探す土俵は、土俵になっていない。** **回数は 1 組で確定でき、時間は組数ぶんの幅つきでしか言えない。**
10. **プロセス間の幅は、セッション間のずれを覆わない。** 同じビルド・同じ手順で日を変えて測ると、分布ごと動く。最も幅の狭い条件 (1 回 1.9 秒、幅 1.2〜1.7%) で、前回が 131.75〜133.39 tok/s、次が 128.83〜130.95 tok/s と **範囲が交わらなかった**。幅が狭い条件ほどセッション間のずれが露出する。**2 つのビルドを比べるなら、同一セッション内でインターリーブして測ること。** 別セッションで測った数字を突き合わせて数 % を語ってはいけない。 **そして同一セッションでも、2 つのビルドの差をソースの差と読むには、同じソースをビルドし直した対照が要る。** 2026-09-23 に踏んだ。cumo の master と 0.10.0 で Mamba が 2.4% 速く (p = 3e-06)、**変わったソースは Mamba のループを 1 度も通らず、GPU の仕事も同じ** だった。**同じ v0.10.0 のソースを gem と rake でビルドしただけで Switch が 1% 動いた。** 投入の多いワークロード (1 トークンに千本単位) ほど、ホスト側のバイナリの違いが wall に出る。**ビルドをまたぐ 1〜2% は、同じソースの再ビルドを並べるまで帰属しないこと。**
11. **単体の演算は 1 プロセス 1 ケースで測り、対照の演算を必ず並べること。** 1 プロセスに複数ケースを詰めると、ケースごとにクロックの立ち上がり具合が違い、それがビルドの順序と相関して **無い差が出る**。**ノイズ床を同時に測っていない単体ベンチの数字を docs に書かないこと。** 2.3〜2.6 us の土俵は cumo の 1 演算あたりの固定費 (起動 + プール + ndloop + Ruby) そのもので、カーネル側の差は原理的に出ない。 **そして同じ配列を読み回す単体ベンチは L2 に当たる。** 2026-09-23 に 2 度踏んだ。13.8 MB の入力を読むコピーを「帯域の天井の 95%」と書いて取り下げ、別セッションは 1 層ぶんの転置を読み回して「転置は直接読むほうが速い」と出した。**実ワークロードと同じく、層ごとに別の配列を順に読む形で測ること。**
12. **出力を毎回確保しないこと。** 確保と managed メモリの初回タッチが両ビルドに乗り、カーネルの差を覆う。**改善を過小評価する向きに出る。** ただし **リポジトリのコードがその形で呼んでいるなら、確保込みの値も併記すること** (`Ops.contiguous` は毎回確保するので、モデル読み込みで実際に払うのは確保込みの側である)。
13. **GPU の仕事を減らす改善は、連続運転でも測り直すこと。** ウォームアップ後の best-of-N は短いバーストなので、**ウォームアップで上がったクロックのまま測れてしまう** おそれがある。**順序を入れ替えない 1 往復は、熱とクロックが実装と相関するので信用しないこと。** 計測は `sudo nvidia-smi -lgc 3090` / `-lmc 14001` でクロックを固定してから行い、固定した旨を数字に添えること (root が要るので既定の利用者が得る値ではない)。
14. **計測の前にクロックの状態を確認し、意図した状態でなければ測らずに止めること。** このカードはメモリクロックが 9001 / 11001 / 14001 MHz を行き来し、読み出し帯域が 413 / 508 / 599.5 GB/s と 1.45 倍動く。**同じコードが状態次第で 1.4 倍違って出る** ので、固定していない数字を別の機会の数字と比べてはいけない。手順は、**計測を始める直前に** 次を叩いて読みを確認する。

```
nvidia-smi --query-gpu=clocks.sm,clocks.mem --format=csv,noheader   # 固定中はアイドルで 2797 MHz, 9001 MHz
```

固定には root が要るので自分では掛けられない。掛けるのは `sudo nvidia-smi -lgc 3090` と `sudo nvidia-smi -lmc 14001` で、**2 つは 1 回ずつ別に実行する** (まとめて渡すと拒否される)。戻すのは `sudo nvidia-smi -rgc` と `sudo nvidia-smi -rmc`。**固定中はアイドルでも高いクロックで回る** ので、計測が終わったら戻すところまで見届けること。

**固定は再起動で外れる。** 2026-09-21 に再起動したらアイドル 450 MHz まで落ちていた。**再起動を挟んだら必ず読み直し、掛け直しを依頼すること。** 掛けっぱなしのつもりで測ると、気づかないまま別の機械を測ることになる。

**固定が掛かっているかどうかは、アイドルの SM で判定できる。** 固定あり 2790〜2805 MHz、固定なし 350〜700 MHz 台とはっきり違う (2026-09-21 に解除直後を読んだら 360 MHz だった)。**固定なしの読みだったら、そこで計測を止めて固定を依頼すること。ただしメモリの段はアイドルでは判定できない。** 固定した直後でもアイドルは 9001 と読む。段を見たいなら **負荷を掛けて、その最中にサンプリングすること**。

**`-lgc 3090` は 3090 MHz を出さない。** 負荷中は 2752〜2805 に散る。**「3090 でないから効いていない」と読まないこと。** メモリの段は電力に付いて動き、**40 W 未満では 14001 に一度も達しない**。14001 が要るのに上がらないときは、固定と戦わずに **走行ごとにクロックをサンプリングして、段を数字に添える** ほうが早い。意図的に別の段で測るときも、**その段を数字に添えて書く**。

15. **外から覗くサンプラは、デューティ比の低い対象では「起きなかった」と「見ていなかった」を区別できない。** サンプラが対象の中に落ちる期待値を先に数えること。**区別するには、測る側のプロセスの中から読むか、対象を長くすること。** クロックに限らず、外部プロセスで採る指標すべてに効く。**目安は「サンプラの間隔を窓の 1/5 以下」にする。** 2026-09-22 に踏んだ。計測区間の窓が **0.09 秒**、`nvidia-smi` が **200 ms 間隔**、**1 窓あたりの期待サンプル数が 0.45** だった。**窓の中の 1 サンプルが低い段を指した回も、速度は高い群のままだった** — 1 サンプルは段の判定にならない。**窓を数秒にするか、間隔を窓の 1/5 以下にすること。**
16. **指標は 2 つ以上見る。** 1 つの指標だけで「効いた / 効かない」を決めない。**単独の指標が期待と違うとき、それは対象が動いていない証拠ではなく、その指標だけでは足りないという合図である。**
17. **「効果なし」と言う前に交絡を外す。** **「効かなかった」は「この交絡が乗ったままでは見えなかった」であることが多い。** 変更の順序が結論を変える。**そして交絡が 1 つとは限らない。同じ大きさのものが 2 つあると、外す順序では解けない。** どちらを先に直しても数字が動かないので、**「直したのに動かない」を「そこは原因ではなかった」と読むと、両方とも取り逃がす。** **そして測る対象のコードを疑うときは、コメントではなく式を読むこと。** コメントは検査されないので、書いた時点の意図しか残らない。
18. **2 つの測定の差から機構を提案しないこと。**「メモリが 14001 のとき SM が落ちるのは電力の取り合いだから」のような説明は、2 条件の差からいくらでも作れる。このカードの governor については 2 セッションが合計 4 回仮説を外している。**読めるのは「条件ごとに違う」までで、なぜそうなるかはその測定では決まらない。** 機構を書くなら、それ自体を独立に測ってから書く。**そして立った機構に満足して探索をやめないこと。** 作法 18 の前半も作法 24 も **機構を立てる側** を戒めているが、**立ってしまったあとのほうが危ない。** **正しい説明ほど危ない** — 誤った説明なら矛盾に当たって探索が続くが、**正しい説明は「もう分かった」で手を止めさせる。** **勝つ形の説明が付いたら、次は負ける形を名指しで探すこと。** 手順は、**「この機構が効かないのはどういう形か」を測る前に言葉にする**。言葉にできるなら測れる。2026-09-20 に踏んだ。`cumsum` の閾値を落とすと実ワークロードで 4.6% 速いと出て、「単体では負けるのに実使用で勝つのは、同期が後ろのキューを待たせるからだ」という **正しい** 説明が付いた。**その時点で形を振るのをやめた。** 別セッションが軸方向を測ったら **行数が多い形で 23 倍の退行** だった。**「同期は行数で償却されるが起動費は償却されない」は、測る前に言えたはずの一文である。**
19. **条件で符号が反転すること自体が、立派な結論である。** ペア比が熱い条件で 1.041 / 1.013 (10/10)、熱くない条件で 1.001 (5/10、1 をまたぐ) のようなとき、これを「全体では効果なし」に丸めないこと。**「どこで効くか」は「効くか」より有用な結論である。**
20. **結果の表には、動かないはずの行を必ず入れること。** 対照を同時に測るだけでなく、**表に載せる**。読む側が幅を目で確かめられる。**そして対照は、結論だけでなく派生量のすべてに当てること。** 表に載せるだけでは足りない。**「動かないはずの行」は、結論に使うすべての量について計算すること。** **そして群にまとめると、動かないはずの行そのものが見えなくなる。** 群をまたぐと 1 対 1 の対応が消え、**数字は同じまま機構だけが隠れる**。**まず生の名前で数えてから群にまとめること。** そして **合計がなぜその値か説明が付かないときは、群の切り方を疑う。** **まとめすぎたかどうかは、群の中を 1 つずつ見るまで分からない。**
21. **主張を書くときは、その主張が何条件・何ラウンドぶんの測定に基づくかを数字と一緒に書くこと。** 測った数字そのものは検証されても、**その数字に乗った主張は素通りする**。「n 条件中 m 条件」「10 ラウンド中 x 回」を主張と同じ文に書けば、受け取る側が根拠の範囲を判断できる。**埋めてから初めて言えることもある。**
22. **ウォームアップはプロセス単位だけでなくバッチ単位にも要る。** プロセスの中で温めても、**バッチの 1 本目は GPU がアイドルから立ち上がる** ので、メモリクロックが低い段のまま走り切ることがある。**捨てるラウンドを 1 本先に回すこと。** 値の大小で捨てるのは恣意的だが、**位置 (最初の 1 本) で捨てるか、段で資格を判定する** のは独立な根拠がある。段は `CLOCKS=1` で計測区間の時刻を出し (`script/clock_window.rb`)、別に回した `nvidia-smi --query-gpu=timestamp,... -lms 200` と突き合わせれば見える。
23. **単体の演算を best-of-N で測った値を、回数で掛けてはいけない。**`best-of-N` は「この操作が最良のときいくらか」を測る道具で、**N 回ぶんの費用を見積もる単価には向かない**。単価に使うなら平均を取り、`Max / Med` が大きければ平均も使えない (作法 9)。**推定量は「何を掛けるか」で選ぶ** — 同じ測定から min / 中央値 / 平均のどれを取るべきかは、その数を **単体で読むのか、回数で掛けるのか** で変わる。**測り方が正しくても、使い方で誤る。**
24. **差を 1 つの要因に帰するなら、その要因だけを足した (または引いた) 対照が要る。**「カーネル数が同じ、答えも同じ、だから違いは同期だけ」は成り立たない。**思いつく交絡が無いことは、交絡が無いことの根拠にならない。** そして **帰属を取り下げるときは、代わりの帰属を置かずに「未分離」と書くこと。** **そして取り下げた直後に出す数字は、取り下げた数字と同じだけ疑うこと。** **過大を取り下げた直後は、小さい数字が安全に見える。** 小さいことは正しさの根拠ではない。**確かめる先は分母である** — その割合は **誰の何に対する割合か**。2026-09-20 に踏んだ。GPT-2 の融合 AdamW を「全体で 1.72 倍」と見積もり、**別の予算と比べていたことに気づいて「PyTorch の実装差で測ると 26%」に訂正した** が、**その 26% は PyTorch の 1 ステップに対する割合** で、しかも PyTorch の比較元 (`foreach`) は **既に複数テンソルをまとめて回す実装** だった。こちらの素の式はそこに来ていないので、**正しくは step の 42%、全体で 1.73 倍** と、最初の見積もりに戻る。**6.6 倍のときは大きすぎて怪しいので検算したのに、26% は落ち着いて見えたので検算しなかった** (作法 20 の末尾と同じ「それらしい値は門番にならない」)。**同じ日に、単位を変えて 2 度やった** — 予算の式 (7P 対 44P) と、割合の分母 (26%)。**どちらも他人の量を自分の量として読んでいる。** 取り下げの文の中で別の帰属を置いて、同じ間違いをもう一度やった実績がある。**主張は数字より速く伝わるので、取り下げは主張を受け取った先にも届けること** (作法 21)。**そして対照を作る前に、予算が足りるかを計算すること。** 変えた部分が 1 トークンの何 % を占めるかは、**触った重みのバイト数と帯域の段から机上で出る**。それが観測した差より小さければ、**その要因を丸ごと 0 にしても差は説明できない** — 測る前に帰属が落ちる。2026-09-20 に踏んだ。Llama 2 の残差を C に置いた件で、10 点が 2 つの塊 (時間にして 7.33% 差) に見えたのを gemm に帰しかけたが、**変えた `wo` の重みは `768 x 768 x 4 x 12 層` = 28.3 MB/token で、最低段 413 GB/s でも 1 トークンの 3.67%、最上段 600 GB/s なら 2.53% しかない**。**段の不確かさを最大に振っても足りない** ので、そこからは出ない。**この計算は無料で、測るより先にできる。** **ただしその予算は、測ったコードの式で計算すること。** 理想化した実装の予算を出して実測と比べると、**その差は非効率ではなく実装の差である** のに、遅さとして読んでしまう。**下限は 2 つ出す** — **アルゴリズムの下限** と、**書いたコードの下限**。両方あって初めて「融合で何倍取れるか」が言える。**積み方は式ではなく演算の数で数える** — 二項演算は 3P (2 読み 1 書き)、スカラー演算と代入は 2P。`a*b + c*d` は 1 式でも 3 演算で 8P である。2026-09-20 に踏んだ。GPT-2 の AdamW で **`g`, `m`, `v`, `p` を 1 度ずつ読んで 3 つ書く 7P = 3.25 GB** を下限として出し、実測 38.4 ms と比べて「底から 7.1 倍」と書いた。**書いた式は融合していないので中間配列を毎回読み書きし、実際は 44P = 21.9 GB、600 GB/s で 36.5 ms。実測との比は 1.05 だった。** **このコードは自分のトラフィックの 5% 以内で走っていた。** 7.1 倍は融合版との差で、遅さではない。**そして結論まで反転した** — 「テンソルを 148 本から 1 本に平坦化するのが先」と書いたが、平坦化で消えるのは起動費だけ (1.17 倍) で、44P は残る。**融合のほうが 6.6 倍で桁が違う。** **7.1 倍という「それらしい値」が出たので門番が働かなかった** (作法 20 の末尾と同じ形)。**そして A/B の前に、測っている対象が出荷されているかを問うこと。** **「A/B が公平か」と「測っている対象が実在するか」は別の問いである。** 同じ 2026-09-20 の件で、1 つのビルドで両アームを回し、同期警告で経路が分かれていることを確かめ、トークン列の一致まで見た。**公平さは丁寧に作った。** そのうえで測った綴りは **計測のためだけに差し戻した古い版** で、**現行のコードはその経路を通らない**。つまりその 4.6% は、**閾値を落としてもこのリポジトリのどこも速くならない**。**丁寧さは油断の場所を変えるだけで、無くしはしない。** **交絡を潰す作業がどれだけ丁寧でも、「その形は通るのか」という別の問いは潰れない。** **いちばん安い門番は、呼び出し元を 1 つ挙げること。** 自分の測定なら「これはどこから呼ばれるか」を書き、他人の主張を受け取ったら「呼び出し元を 1 つ教えてください」と聞く。**実装に入る前に決着する。** そして docs には「計測のあいだだけ差し戻した」と書いてあったのに、**そこから出る結論は書いていなかった** — **材料を渡すことと結論を渡すことは別である。**

## テストの作法

- 数値一致の検証は二段構え:
  1. Numo vs llm.c の参照値 (実装が正しいか)
  2. Cumo vs Numo (バックエンド間の一致)
- 許容誤差は「なぜその値か」をコメントに書く。根拠なく緩めない。argmax の一致・トークン列の一致のような **完全一致を要求できる検査** を優先する。
- 乖離が出たら、最終出力ではなく **層ごとの中間活性** で最初に乖離した場所を特定する。そのための比較関数を第一段階から用意しておく。

## 環境

- 開発機の GPU は RTX 5070 Ti Laptop (Blackwell, sm_120)。cumo のビルドで JIT を避けるなら `CUMO_NVCC_GENERATE_CODE=arch=compute_120,code=sm_120`。
- ラップトップなのでサーマルで数字が落ちる。長いベンチは `nvidia-smi --query-gpu=clocks.sm,temperature.gpu --format=csv -l 1` を並走させて確認する。
- **帯域の天井はメモリクロックの状態で変わる。** このカードは 9001 / 11001 / 14001 MHz を行き来し、読み出し帯域が 413 / 508 / 599 GB/s と 1.45 倍動く (`clocks.max.mem` は 14001)。**単独のマイクロベンチは 11001 から上がらないのに、生成ループは 14001 に達する** ので、両者の GB/s を突き合わせるなら **実行中の** クロックを採ること (終わった後の 1 サンプルでは足りない)。揃えたいときは `sudo nvidia-smi -lgc 3090` と `sudo nvidia-smi -lmc 14001` (2 つは別々に実行する)、戻すのは `-rgc` と `-rmc`。固定中はアイドルでも最大クロックで回るので戻し忘れないこと。
- **電力は効いている。サーマルは効いていない。** 持続的な負荷では **クロックを固定してもしなくても中央値 99.4 W** で、枠の 100 W に張り付く。以前ここには「中央値 39 W で枠に届かない」「BIOS 側に触る余地は無い」と書いていたが、**39 W は再現しなかった**。温度は 62〜64 ℃ で HW Thermal Slowdown は出ず、Resizable BAR も既に有効。**効くのはクロックの固定** (融合版の decode で +8.6〜9.6%、融合なしでは +0.2〜2.1%) と、**まだ試していない `-pl`** (`power.max_limit` は 140 W)。固定が変えるのは消費量ではなく **メモリと SM のどちらに回すか** である (キャッシュ無し 256 で、固定なしがメモリ 11001・SM 2115、固定ありがメモリ 14001・SM 1990、電力はどちらも 99.4 W)。
- **`/tmp` は tmpfs なので再起動で消える。** セッションのスクラッチパッドもそこにあるので、**計測の中間結果を置いたまま再起動すると失う** (2026-09-21 に、回している最中の 2 バッチを丸ごと失った)。**残す価値のある数字は、集計してリポジトリ側 (docs/results/) に書くまでが 1 単位である。**
- **cuDNN のワークスペース上限は cumo 0.10.0 で既定 128 MiB になった** (0.9.0 までは 8 MiB で、2019 年から一度も動いていなかった)。超えるアルゴリズムは `cudnnFindConvolutionForwardAlgorithmEx` の候補に入らない。`CUMO_CUDNN_MAX_WORKSPACE_SIZE` に正の 10 進バイト数で渡すと振れる。**0.9.0 の既定から 128 MiB へ上げると ResNet-18 が 1.51 倍**、さらに `CUMO_ALLOW_TF32=1` で 1.07 倍だが **logits が 2 桁動く** (docs/results/resnet-18.md)。**畳み込みを測るなら、上限と TF32 の両方を条件として書き添えること。**
- **単精度がテンソルコアに載るかは `CUMO_ALLOW_TF32` で、0.10.0 からは `gemm` にも掛かる** (既定は off でビット一致)。**`Cumo.allow_tf32?` で読める。** 0.9.0 以前は畳み込みが黙って載っていた。
- **デバイスからの読み戻しは 0.10.0 で 8 倍安くなった** (`extract_cpu` が decode の形で 143.8 us から 18.1 us)。**それでも 1 トークンに 18 us 残る** ので、ループの中の読み戻しは 1 回までにするという方針は変わらない (docs/cumo-history.md)。
- **ベンチのスクリプトのオプションは 2 語に固定してある。**`--rounds N` が best-of-N の N (既定 3、環境変数 `REPEAT` でも指定できる)、`--inner N` が **計測の区間の中で通す回数**。Python 側の `python/bench_*.py` も同じ綴りを受ける。**`--repeat` は別名として残してあるが、意味がスクリプトによって違っていた語なので、新しく書くときは使わないこと。**
- **短い条件は `--rounds` を上げて、1 プロセスが 2 秒以上連続で回るようにすること。** 計測区間が 0.1 秒だと **メモリクロックの段が上がる前に終わり、走行ごとに 9001 と 14001 のどちらかに落ちる** (docs/cumo-history.md)。GPT-2 の 64 トークンで `REPEAT=3` が 807 / 617 / 617、`REPEAT=30` が 817 / 815 / 817 になる。
- デバッグ用環境変数:
  - `CUMO_SHOW_WARNING=ON CUMO_SHOW_WARNING_ONCE=OFF` — 同期の警告
  - `CUDA_LAUNCH_BLOCKING=1` — 常時同期 (プロファイル用、遅くなる)
  - `CUMO_MEMORY_POOL=OFF` — メモリプール無効化
